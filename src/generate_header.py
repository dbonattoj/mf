#!/usr/bin/python

import os
import glob

headers = []
for root, dirnames, filenames in os.walk("."):
	directory_headers = glob.glob(root + "/*.h")
	for h in directory_headers:
		headers.append(h[2:])

f = open("mf.h", "w")
f.writelines([
	"// File automatically generated by generate_header.py\n",
	"\n",
	"#ifndef MF_H_\n",
	"#define MF_H_\n",
	"\n"
])

for h in headers:
	if(h != "mf.h"):
		f.write("#include \"" + h + "\"\n")

f.writelines([
	"\n",
	"#endif\n"
])
